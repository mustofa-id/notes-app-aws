/**
 * @typedef {{
    id: string,
    title: string,
    createdAt: string,
    updatedAt: string,
    tags: string[],
    body: string,
  }} Note
 */

/** @type {Array<Note>} */
const notes = []

module.exports = notes
